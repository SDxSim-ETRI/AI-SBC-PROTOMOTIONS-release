*/babel/*.json
.idea
.vscode
*__pycache__*
*.egg-info
# Build artefacts and downloaded wheels. `python -m build` writes here, and a
# stray `pip download` in the working tree is otherwise one `git add -A` away
# from committing an 11 MB binary that no history rewrite gets back out.
*.whl
dist/
outputs/*
output/*
rendered_images/*
*tmp*
results/*
exps/*
.DS_Store
**/.DS_Store
wandb/*
newton_source/*
data/amass/*.pkl
.claude/*
!.claude/commands/
.cursor/
renders/
.envrc
CLAUDE.local.md
# safety-probe traces (multi-GB npz)
.cache/

# Newton/IsaacLab viewer window state; churns on every run
imgui.ini
zarr_data/
*.zarr/
*.zarr.zip

# ── 커밋 금지 (2026-09-01) ──────────────────────────────────────────────────
# SMPL 바디모델은 Max Planck 라이선스로 **재배포 금지**다. 렌더에 필요해 이 서버에는
# 두지만 절대 커밋하지 않는다. (실제로 공개 저장소에 9분간 올라간 적이 있다)
etri_라이선스동의_다운로드/
# 추론·렌더 산출물 — 릴리즈 대상이 아니다
recordings/
